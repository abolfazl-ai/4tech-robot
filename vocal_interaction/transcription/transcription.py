def transcribe():
    # Play beep sound
    # Record the audio until user pauses. You can use speech_recognition.Recognizer() for that
    # language = language_id.identify_language(audio)
    # if robot is online -> text = online_transcription.transcribe_online(audio, language)
    # else -> text = offline.offline_transcription.transcribe_offline(audio, language)
    # return (text, language)
    pass
